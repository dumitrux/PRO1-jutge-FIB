//I-èsim (1)
#include <iostream>
using namespace std;


int main() {
    int n, seq, pos = 1;
    cin >> n;
    while (cin >> seq and pos != n and seq != -1) {
        ++pos;
        
    }
    cout << "A la posicio " << n << " hi ha un " << seq << "." << endl;
}
